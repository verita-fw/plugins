#
# This file is automatically generated. Do not manually edit this file - changes *will* be lost!
#
{% if helpers.exists('Muro.NodeExporter.enabled') and Muro.NodeExporter.enabled == '1' %}

{%- set collector = "--collector." -%}
{%- set no_collector = "--no-collector." -%}

{%- if helpers.empty('Muro.NodeExporter.cpu') -%}
    {%- set cpu = no_collector + "cpu " -%}
{%- endif -%}

{%- if helpers.empty('Muro.NodeExporter.exec') -%}
    {%- set exec = no_collector + "exec " -%}
{%- endif -%}

{%- if helpers.empty('Muro.NodeExporter.filesystem') -%}
    {%- set filesystem = no_collector + "filesystem " -%}
{%- endif -%}

{%- if helpers.empty('Muro.NodeExporter.loadavg') -%}
    {%- set loadavg = no_collector + "loadavg " -%}
{%- endif -%}

{%- if helpers.empty('Muro.NodeExporter.meminfo') -%}
    {%- set meminfo = no_collector + "meminfo " -%}
{%- endif -%}

{%- if helpers.empty('Muro.NodeExporter.netdev') -%}
    {%- set netdev = no_collector + "netdev " -%}
{%- endif -%}

{%- if Muro.NodeExporter.ntp == '1' -%}
    {%- set ntp = collector + "ntp " -%}
{%- endif -%}

{%- if helpers.empty('Muro.NodeExporter.time') -%}
    {%- set time = no_collector + "time " -%}
{%- endif -%}

{%- if Muro.NodeExporter.devstat == '1' -%}
    {%- set devstat = collector + "devstat " -%}
{%- endif -%}

{%- if helpers.empty('Muro.NodeExporter.zfs') -%}
    {%- set zfs = no_collector + "zfs " -%}
{%- endif -%}

node_exporter_args="{{ cpu }}{{ exec }}{{ filesystem }}{{ loadavg }}{{ meminfo }}{{ netdev }}{{ ntp }}{{ time }}{{ devstat }}{{ zfs }}"
node_exporter_listen_address="{{ Muro.NodeExporter.listenaddress }}:{{ Muro.NodeExporter.listenport }}"
node_exporter_enable="YES"

{%- else -%}

node_exporter_enable="NO"

{%- endif -%}
